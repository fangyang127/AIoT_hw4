tensorflow
pillow
streamlit
numpy
pandas
matplotlib

# NOTE: TensorFlow is left unpinned to allow the deployment environment
# to choose a compatible version. If you prefer to pin a version,
# set the desired version (e.g. `tensorflow==2.13.0`).
